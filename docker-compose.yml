version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: teamboard-db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - teamboard-network

networks:
  teamboard-network:
    driver: bridge

volumes:
  mongodb_data:
